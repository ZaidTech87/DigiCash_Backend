/**
 * Spring configuration classes (beans, CORS, JPA auditing, etc.).
 *
 * Empty as of Phase 1 (project foundation only) - reserved for later
 * phases. This file exists so the package itself is tracked by version
 * control, since Git does not track empty directories.
 */
package com.digicash.backend.config;
