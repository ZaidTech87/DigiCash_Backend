spring.application.name=digicash-backend

# ---------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------
server.port=${PORT:8080}
server.address=0.0.0.0
# ---------------------------------------------------------------------
# MySQL - Aiven
# ---------------------------------------------------------------------
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=REQUIRED&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------------------------------------------------------------------
# JPA / Hibernate
# ---------------------------------------------------------------------
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:validate}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.open-in-view=false

# ---------------------------------------------------------------------
# Flyway
# ---------------------------------------------------------------------
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=false











#BEFORE  deployement code
## DIGICASH Backend - main configuration
##
## All environment-specific values (database host, credentials, port) are
## read from environment variables with safe local-development fallbacks.
## NEVER hardcode a real password here - production values must always be
## supplied via environment variables (or a secrets manager / orchestrator
## config in a real deployment), never committed to source control.
#
#spring.application.name=digicash-backend
#
## ---------------------------------------------------------------------
## Server
## ---------------------------------------------------------------------
#server.port=${SERVER_PORT:8080}
#
## ---------------------------------------------------------------------
## MySQL datasource
## ---------------------------------------------------------------------
## DB_PASSWORD intentionally defaults to an empty string, not a real
## secret - this only matches a local MySQL root account with no password
## set, which is a common fresh-install default. Any real environment MUST
## set DB_PASSWORD explicitly.
#spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:digicash}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=Dt@123
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
## ---------------------------------------------------------------------
## JPA / Hibernate
## ---------------------------------------------------------------------
## ddl-auto is 'validate', NOT 'update' or 'create' - Flyway (below) is the
## single source of truth for the schema. Hibernate only checks that the
## entity mappings agree with whatever schema Flyway has already applied;
## it never creates or alters a table itself. This prevents the two
## mechanisms from ever fighting over who owns the schema.
#spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:validate}
#spring.jpa.show-sql=${JPA_SHOW_SQL:false}
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#spring.jpa.properties.hibernate.jdbc.time_zone=UTC
#spring.jpa.open-in-view=false
#
## ---------------------------------------------------------------------
## Flyway
## ---------------------------------------------------------------------
#spring.flyway.enabled=true
#spring.flyway.locations=classpath:db/migration
#spring.flyway.baseline-on-migrate=false
#isme kya change karu


